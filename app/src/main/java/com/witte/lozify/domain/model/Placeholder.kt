package com.witte.lozify.domain.model

/**
 * Placeholder file for domain models (pure Kotlin).
 *
 * ⚠️ CRITICAL: This package MUST remain pure Kotlin with ZERO Android dependencies.
 * ❌ FORBIDDEN: android.*, androidx.*, kotlinx.android.*
 * ✅ ALLOWED: kotlin.*, kotlinx.coroutines.*, java.time.*
 *
 * This package will contain:
 * - Note.kt (domain model for notes)
 * - Tag.kt (domain model for tags)
 * - Attachment.kt (domain model for images)
 * - NoteRelation.kt (domain model for @mentions)
 *
 * Stage 1 will implement these pure domain models.
 */
